{
  "name": "@jarzt/validate-simple-type",
  "version": "0.0.1",
  "description": "Provide simple methods to validate primitive type",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --build tsconfig.json",
    "publish": "npm publish --access public"
  },
  "author": "Jarzt",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.3"
  }
}
